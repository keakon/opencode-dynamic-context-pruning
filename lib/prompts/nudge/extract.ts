export const NUDGE_EXTRACT = `<instruction name=context_management_required>
Context is filling with tool outputs. After the current atomic step, extract key findings you must preserve and remove the raw outputs.
</instruction>`
